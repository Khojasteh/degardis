trigger: >-
  The manifest declares no interface.
impact: >-
  The bundle carries no display metadata, so a host has no name or summary to show. Declare interface with display_name, short_description, and default_prompt.
