trigger: >-
  The manifest holds a field this compiler does not define.
impact: >-
  The field is ignored, so it cannot configure the skill. Only fields defined by the manifest schema have effect; construct definitions live in their selected source files.
