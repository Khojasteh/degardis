# Clear reporting: worked examples

Optional explanation. Nothing here is required.

Leading with the result:

> The migration succeeded for 9 of 10 tenants. Tenant `acme` is unchanged
> because its schema predates the column the migration renames.

Naming what was not covered:

> The appendix on billing is not summarized: it contradicts the pricing section,
> and the material does not say which is current.
