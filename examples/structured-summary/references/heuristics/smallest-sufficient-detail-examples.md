# Smallest sufficient detail: worked examples

Optional explanation. Nothing here is required.

- A benchmark table became one sentence naming the regression and its size,
  because the reader was deciding whether to roll back.
- A qualification about sample size stayed, because dropping it would have made
  a tentative result read as settled.
