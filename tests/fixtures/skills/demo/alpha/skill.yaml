name: alpha
format_version: 2
version: 1.2.3
license: MIT
copyright: Copyright (c) 2026 Alpha
description: Exercise every Format 2 construct in one bounded task.
primary_workflow: run
policies:
- run-authority
protocols:
- run-trail
guidance:
- run-context
content:
  policies:
  - policies/*.yaml
  rules:
  - rules/*.yaml
  patterns:
  - patterns/*.yaml
  heuristics:
  - heuristics/*.yaml
  guidance:
  - guidance/*.yaml
  protocols:
  - protocols/*.yaml
  records:
  - records/*.yaml
  workflows:
  - workflows/*.yaml
  profiles:
  - profiles/*.yaml
  references:
  - references/**/*.md
  scripts:
  - scripts/*.py
  assets:
  - assets/*.md
interface:
  display_name: Alpha
  short_description: Exercise every construct
  default_prompt: Use {name} for this task.
