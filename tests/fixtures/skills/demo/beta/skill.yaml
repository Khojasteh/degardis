name: beta
format_version: 2
version: 0.2.0
description: A second skill, so discovery and multi-skill builds have one to find.
principles:
- reporting
tasks:
- note
content:
  tasks:
  - tasks/*.md
  principles:
  - principles/*.md
interface:
  display_name: Beta
  short_description: A second skill
  default_prompt: Use {name} for the second job.
