name: beta
format_version: 2
version: 0.9.0
description: A second skill, small enough to compile in one step.
primary_workflow: run
content:
  records:
  - records/*.yaml
  workflows:
  - workflows/*.yaml
interface:
  display_name: Beta
  short_description: Compile in one step
  default_prompt: Use {name} once.
